[tool:pytest]
testpaths = tests          # Look for tests in the 'tests' directory
python_paths = .           # Add project root to Python path (helps with imports)
addopts = -v --tb=short    # Verbose output, short tracebacks
asyncio_mode = auto        # Automatically handle async tests